const oneInch = require(''); // placeholders for dex controller
const uniswap = require(''); // placeholders for dex controller

const supportedDex = [ 'oneInch', 'uniswap' ];

module.exports = {supportedDex, uniswap, oneInch};